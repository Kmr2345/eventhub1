name: eventhub
description: Astana IT University — Student Events App

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2
  provider: ^6.1.2
  google_fonts: ^6.1.0
  qr_flutter: ^4.1.0
  intl: ^0.19.0
  cached_network_image: ^3.3.1
  share_plus: ^10.0.0
  path_provider: ^2.1.5
  url_launcher: ^6.3.0
  http: ^1.2.0
  font_awesome_flutter: ^10.7.0
  mobile_scanner: ^5.2.3
  image_picker: ^1.1.2
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true